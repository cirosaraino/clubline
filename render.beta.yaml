services:
  - type: web
    name: clubline-backend-dev
    runtime: node
    plan: starter
    rootDir: backend
    buildCommand: npm ci --include=dev && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: APP_ENV
        value: dev
      - key: NODE_ENV
        value: development
      - key: ENABLE_LOCAL_REALTIME_FALLBACK
        value: false
      - key: ENABLE_LEGACY_WORKFLOW_FALLBACK
        value: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false

  - type: web
    name: clubline-web-beta
    runtime: static
    buildCommand: ./scripts/render-build-web.sh
    staticPublishPath: build/web
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: APP_ENV
        value: dev
      - key: SKIP_INSTALL_DEPS
        value: true
      - key: API_BASE_URL
        sync: false
      - key: REALTIME_TRANSPORT
        value: supabase
